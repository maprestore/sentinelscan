{
  "name": "sentinelscan",
  "version": "0.5.0",
  "private": true,
  "description": "A bounded, non-destructive web security posture scanner for authorized targets.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "scan": "node src/cli.mjs",
    "dashboard": "node src/server.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
